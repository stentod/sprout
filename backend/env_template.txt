# Database Configuration
DATABASE_URL=postgresql://username@localhost/sprout_budget

# Email Configuration for Password Reset
MAIL_SERVER=smtp.gmail.com
MAIL_PORT=587
MAIL_USE_TLS=True
MAIL_USE_SSL=False
MAIL_USERNAME=your-email@gmail.com
MAIL_PASSWORD=your-app-password
MAIL_DEFAULT_SENDER=your-email@gmail.com

# App Configuration
SECRET_KEY=your-secret-key-here
FLASK_ENV=development

# Instructions:
# 1. Copy this file to .env: cp env_template.txt .env
# 2. Update the values above with your actual configuration
# 3. For Gmail, use an App Password instead of your regular password
# 4. To generate a Gmail App Password:
#    - Go to Google Account settings
#    - Security > 2-Step Verification > App passwords
#    - Generate a new app password for "Mail"
